# נקודת בקרה — v39 / d39

תיוג: `v2-d39`. לחזרה למצב הזה: `git checkout v2-d39`.

## שתי אפליקציות, מנוע אחד

**`/` — התצוגה המפורטת (v39).** סבבים מעוגנים, ממוצעים מתגלגלים,
טבלאות חישוב, שיטות TDEE, גרפים מלאים. חמישה טאבים.

**`/v2/` — הלוח (d39).** אותם נתונים בשפה פשוטה. ארבעה טאבים:
סיכום, הזנה, משקל, יעדים.

`v2/index.html` טוען את `../js/lib` ו-`../js/core` במקום להעתיק,
ולכן תיקון בחישוב חל על שתיהן. רק שכבת התצוגה נכתבה מחדש.

## הגיליון — הפרוטוקול המדויק

הסקריפט הקיים תומך בכל מה שצריך; אין צורך לשנות בו דבר.

| פעולה | כיוון | פרמטרים |
|---|---|---|
| `get` | קריאה | — |
| `getNutrition` | קריאה | — |
| `add` | כתיבה | `date, weight, muscle, fat, fluids` |
| `addNutrition` | כתיבה | `date, calories, fat, carbs, protein, fiber, steps` |

**שלושה כללים שנקנו בדם:**

**התאריך הוא `DD/MM/YYYY`.** שליחת `YYYY-MM-DD` מפילה את הסקריפט,
והוא מחזיר דף HTML במקום JavaScript — כישלון שנראה בדיוק כמו בעיית
הרשאות ושלח אותנו לחפש שלושה סבבים במקום הלא נכון.

**כל הפרמטרים נשלחים, גם ריקים.** חסר אחד והסקריפט זורק שגיאה.

**JSONP ולא POST.** בקשה חוצת־מקורות ל-Apps Script גוררת preflight
שהוא אינו עונה עליו, ומכאן "Failed to fetch". תג script לכתובת GET
אינו כפוף ל-CORS.

**ו-`onload` אינו עדות להצלחה:** תג script יורה אותו גם על דף HTML
שנכשל בפענוח. ההצלחה היחידה שנחשבת היא קריאה בפועל ל-callback.

## החלטות חישוב

**"כמה ירדתי"** — חלוקת כל התקופה לשניים והשוואת ממוצעים. לא המרחק
בין הקצוות (מנפח) ולא שבוע מול שבוע (מתעלם מרוב הנתונים).

**סבבים מעוגנים ליום הראשון**, לא לתאריך הסיום — אחרת כל החלונות
זזים בכל יום שעובר.

**ההוצאה לא זזה עם הקצב שנבחר.** היא נמדדת ממה שקרה; רק היעד זז.

**מיפוי עמודות לפי שם ולא לפי מיקום**, כשיש שורת כותרות.

## הערכת ארוחה מתמונה

שני מודלים עם הטיות מנוגדות, סיבוב תגובה כשהפער עולה על 12%,
והכרעה בקוד לפי כלל גלוי. מודל קטן מוסר הערכה אך אינו מתווכח.
ספק שנופל אינו מפיל — הצד ששרד ממלא את מקומו.

## בדיקות

```
node tests/run-tests.js     186   מנוע החישוב
node tests/smoke.js         115   ממשק האפליקציה המפורטת
node v2/smoke.js             53   ממשק הלוח
node v2/estimate.test.js     54   הערכה מתמונה
node v2/sync.test.js         15   סנכרון הגיליון
node v2/paste.test.js        11   פענוח הדבקה
node v2/render.test.js        9   עמידות התצוגה
                            443   סך הכל
```

`npm test` מריץ את כולן. `npm run stamp` מטביע חותמת גרסה
בכתובות הקבצים — בלעדיה הדפדפן מגיש קבצים ישנים מהמטמון.

## מה נשאר פתוח

מפתח ה-GitHub וה-OpenRouter עברו בצ'אט וכדאי לבטל אותם.
המכסה החינמית של OpenRouter נגמרת מהר; Gemini נדיב בהרבה.
