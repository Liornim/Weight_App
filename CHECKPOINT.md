# נקודת בקרה — v39 / d69

תיוג: `v2-d69`. לחזרה: `git checkout v2-d69`.

זו הנקודה שלפני צמצום הטאבים. תשעה טאבים, וכולם עובדים.

## שתי אפליקציות, מנוע אחד

**`/` — התצוגה המפורטת (v39).** סבבים, ממוצעים מתגלגלים, שיטות TDEE.

**`/v2/` — הלוח (d69).** תקציב, המצב, ראשי, סיכום, הזנה, משקל,
יעדים, החישוב, מאזן, נתונים.

`v2/index.html` טוען את `../js/lib` ו-`../js/core`, ולכן תיקון
בחישוב חל על שתיהן.

## מה נמדד על 55 ימי נתונים אמיתיים

**חלון קצר אינו מודד מטבוליזם.** חלון של שבוע נתן "תחזוקה" שנעה
בין 1,326 ל-3,609 — פי 2.7 — בעוד חלוקה של התקופה לחצי נתנה תשובה
אחת. הסיבה היא תנודה יומית של 638 גרם, שגדולה מהאות בכל חלון קצר.

**ההתכנסות לפי אורך:**

```
 7 ימים   פער 2,267
10 ימים   פער   649
14 ימים   פער   267
27 ימים   תשובה אחת
```

**התחזוקה שנמדדה: 2,400-2,500 בלי הליכה.** שלוש חלוקות עצמאיות
— חצי, שליש ורבע — התכנסו לשם.

**שלושה תיקונים נוסו ונכשלו**, וכולם מתועדים כאן כדי שלא ינוסו שוב:
מים מגליקוגן (החמיר), פירוק לשומן ושריר לפי מקדמי אנרגיה (החמיר
בהרבה), והסרת קצוות מכל חלון (ללא שינוי). הבעיה אינה בעיבוד אלא
בכך שאין מספיק אות בחלון קצר.

**מדידת ההרכב אינה מדידה עצמאית.** המשקל נמדד; השומן, השריר והמים
מוסקים מאותה מדידת התנגדות. בשבוע אחד המכשיר דיווח "שומן −1.73,
שריר +0.80" — בלתי אפשרי פיזיולוגית, וסימן שהמסה עוברת בין
קטגוריות לפי מצב הנוזלים.

## הגיליון — הפרוטוקול המדויק

| פעולה | כיוון | פרמטרים |
|---|---|---|
| `get` | קריאה | — |
| `getNutrition` | קריאה | — |
| `add` | כתיבה | `date, weight, muscle, fat, fluids` |
| `addNutrition` | כתיבה | `date, calories, fat, carbs, protein, fiber, steps` |

**התאריך הוא `DD/MM/YYYY`.** שליחת `YYYY-MM-DD` מפילה את הסקריפט,
שמחזיר דף HTML במקום JavaScript — כישלון שנראה בדיוק כמו בעיית
הרשאות.

**כל הפרמטרים נשלחים, גם ריקים.** חסר אחד והסקריפט זורק שגיאה.

**JSONP ולא POST.** בקשה חוצת־מקורות ל-Apps Script גוררת preflight
שהוא אינו עונה עליו.

**`onload` אינו עדות להצלחה** — תג script יורה אותו גם על דף HTML
שנכשל בפענוח. רק קריאה בפועל ל-callback נחשבת.

## החלטות חישוב

**המשקל של הבוקר סוגר את היום שלפניו.** לחלון של N ימי אוכל דרושות
N+1 שקילות. היום הנוכחי כמעט תמיד "פתוח" — נשקל, והאוכל שלו ייכנס
מחר — וזה מבנה ולא חוסר.

**רגרסיה על כל השקילות בטווח, לא שתי נקודות קצה.** שתי נקודות
נושאות את מלוא רעש השקילה; במשקל שמתנדנד בקילו בין בקרים זה ייצר
תחזוקה של 882 ליד 3,184 באותה טבלה.

**הפער מול היעד הוא זהות, לא מדידה:**

```
פער = גירעון + הליכה − מה שהמשקל מראה
```

מה שאכלת מצטמצם משני הצדדים, ולכן טעות ברישום הקלוריות אינה
משפיעה עליו כלל.

**22 צעדים לקלוריה** (0.045), כ-450 ל-10,000 צעדים. היה 25. שני
מקורות עצמאיים הצביעו לאותו כיוון: הספרות נותנת 450-470 למשקל של
כ-90 ק"ג, ובנתונים נמצאה הטיה שיטתית בתקופות של הליכה רבה.

**מקור אחד לכל קבוע.** רשימת החלונות הייתה משוכפלת בארבעה קבצים,
קבוע הצעדים בשמונה, ומנוע החישוב נקרא משני מקומות שונים — כל אחד
מהם ייצר מסכים שלא הסכימו זה עם זה. לכל אחד יש עכשיו מקור יחיד
ובדיקה שנכשלת אם עותק יחזור.

## בדיקות

```
node tests/run-tests.js     227   מנוע החישוב
node tests/smoke.js         115   ממשק האפליקציה המפורטת
node v2/smoke.js            117   ממשק הלוח
node v2/estimate.test.js     54   הערכה מתמונה
node v2/sync.test.js         15   סנכרון הגיליון
node v2/paste.test.js        11   פענוח הדבקה
node v2/render.test.js        9   עמידות התצוגה
                            548   סך הכל
```

`npm test` מריץ הכל. `npm run stamp` מטביע חותמת גרסה — בלעדיה
הדפדפן מגיש קבצים ישנים מהמטמון.

## מה נשאר פתוח

הטוקנים שעברו בצ'אט כדאי לבטל.
המכסה החינמית של OpenRouter נגמרת מהר; Gemini נדיב בהרבה.
